overwrite: true
schema: http://localhost:${API_PORT:3000}/graphql
documents: "**/*.graphql"
generates:
  generated/typings/graphql-schema.d.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
