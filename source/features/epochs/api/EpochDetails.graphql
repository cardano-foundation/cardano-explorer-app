#import "./EpochOverview.graphql"
#import "../../blocks/api/BlockOverview.graphql"

fragment EpochDetails on Epoch {
  ...EpochOverview
  blocks(
    limit: 20
    order_by: { number: desc_nulls_last }
  ) {
    ...BlockOverview
  }
}
