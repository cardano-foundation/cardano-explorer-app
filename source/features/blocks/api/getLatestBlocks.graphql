#import "./BlockOverview.graphql"

query getLatestBlocks(
  $limit: Int,
) {
  blocks(
    limit: $limit,
    order_by: {
      number: desc_nulls_last
    },
    where: {
      number: {
        _is_null: false
      }
    }
  ) {
    ...BlockOverview
  }
}
