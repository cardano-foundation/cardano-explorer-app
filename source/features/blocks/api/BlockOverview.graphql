fragment BlockOverview on Block {
  createdAt
  createdBy
  epochNo,
  hash
  number
  size
  slotWithinEpoch
  transactions_aggregate {
    aggregate {
      count
      sum {
        totalOutput
      }
    }
  }
}
