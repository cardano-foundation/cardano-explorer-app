fragment BlockOverview on Block {
  createdAt
  createdBy
  epochNo,
  id
  number
  size
  slotWithinEpoch
  transactions_aggregate {
    aggregate {
      count
      sum {
        totalOutput
      }
    }
  }
}
