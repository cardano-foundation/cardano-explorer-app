#import "./BlockInfo.graphql"

fragment BlockOverview on Block {
  ...BlockInfo
  createdAt
  createdBy
  epoch {
    number
  },
  transactions_aggregate {
    aggregate {
      count
      sum {
        totalOutput
      }
    }
  }
}
