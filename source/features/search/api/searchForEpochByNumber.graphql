#import "../../epochs/api/EpochDetails.graphql"

query searchForEpochByNumber (
  $number: Int!
) {
  epochs (
    where: {
      number: {
        _eq: $number
      }
    }
  ) {
    ...EpochDetails
  }
}
