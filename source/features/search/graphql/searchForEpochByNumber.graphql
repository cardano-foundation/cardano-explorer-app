query searchForEpochByNumber (
  $number: Int!
) {
  epochs (
    where: {
      number: {
        _eq: $number
      }
    }
  ) {
    endedAt
    startedAt
    output
    transactionsCount
    slots {
      number
    }
    blocks {
      id
    }
  }
}
