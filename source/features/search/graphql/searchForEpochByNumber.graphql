#import "../../epochs/graphql/fragments/epochDetails.graphql"

query searchForEpochByNumber (
  $number: Int!
) {
  epochs (
    where: {
      number: {
        _eq: $number
      }
    }
  ) {
    ...EpochDetails
  }
}
