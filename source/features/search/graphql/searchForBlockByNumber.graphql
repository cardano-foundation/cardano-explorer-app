#import "../../blocks/graphql/fragments/blockDetails.graphql"

query searchForBlockByNumber(
  $number: Int!
) {
  blocks(
    limit: 1,
    where: {
     number: {
       _eq: $number
     }
    }
  ) {
    ...BlockDetails
  }
}
